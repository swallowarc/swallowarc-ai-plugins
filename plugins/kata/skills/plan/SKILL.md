---
name: plan
description: 承認済みの設計や spec があるマルチステップのタスクで、コードに触る前に使う。小さく独立に検証可能なタスクからなる実装プランを作成する
---

# plan（実装計画を最小タスクへ分解する）

## 概要

plan は、承認済みの設計や要求定義を、実装者が契約から実装を導ける粒度のタスク列に変換するスキルである。想定読者は「このコードベースの文脈を一切持たない熟練エンジニア」であり、腕はあっても、このプロジェクトの流儀もクセも知らない。触るファイル・満たすべきインターフェース・検証すべき挙動・実行して確認するコマンドを、プランの中に全部書く。「何を満たすべきか」を読み手に推測させてはならない。「どう実装するか」は読み手（implementer）の裁量である。

プラン全体を貫く原則は次の4つである。

- **DRY** — 同じ説明・同じ宣言を複数タスクに重複させない
- **YAGNI** — 今回のスコープに要らない機能をタスクに混ぜない
- **TDD** — コードを書くタスクは受け入れ基準を RED-GREEN で 1 挙動ずつ消化する
- **高頻度コミット** — タスクの内部でも、意味のある単位ごとにコミットを刻む

## 保存先

プランは `docs/kata/plans/YYYY-MM-DD-<feature-name>.md` に保存する。ユーザーが別の保存先を指定した場合はそちらを優先する。

## spec 受け入れ検査

plan 着手の最初のアクションとして、入力の spec を検査する。

1. **証跡の確認** — spec に「検討済み事項」節があるか。なければ未確定点の棚卸しの証跡が確認できない spec である。拒否はせず、「このまま plan に進めるか、`kata:design` で棚卸しを行うか」をユーザーに確認する
2. **内容の検査** — spec を通読し、設計判断を左右する未確定・複数に読める記述・矛盾がないかを確認する。観点は棚卸しと同じレンズ（境界値・失敗時の挙動・同時実行・互換性・性能・外部依存の実挙動）を使う。検討済み事項に載っている項目は未確定として問い直さないが、spec 本文との矛盾を見る検査からは除外しない

見つけた未確定を自分の推測で埋めてタスク化することは、プレースホルダと並ぶプランの欠陥である。見つけた項目は着手前に一度だけまとめてユーザーに問い（タスク作成中に気づくたびに割り込まない）、回答が設計の根幹を変える場合は plan を続けず `kata:design` への差し戻しを提案する。根幹を変えない回答は、plan を続ける前に spec の「検討済み事項」（必要なら本文も）へ反映し、反映箇所が spec の他の記述と矛盾しないことを確認する。spec に反映されていない決定を plan だけが持つ状態を作らない。

## スコープチェック

着手前に、対象の spec が複数の独立したサブシステムにまたがっていないかを確認する。またがっている場合、1 本のプランに詰め込まず、サブシステムごとにプランを分割する。分割の基準は「そのプラン単独で動作確認とテストが完結するか」であり、完結しないなら境界の引き方が誤っている。

## ファイル構成の先行設計

タスクを定義する前に、今回の実装で新規作成・変更するファイルを洗い出し、各ファイルの責務を一覧化する。

- 1 ファイルには 1 つの責務だけを持たせる。複数の役割を担わせない
- 既存コードに手を入れる場合、まずそのプロジェクトの流儀を確認し、無関係な範囲までついでに整理しない
- この一覧がそのままタスク分割の下敷きになる。ファイル一覧を作らずにタスクへ進まない

## タスクの適正サイズ

タスクは「独自のテストサイクルを持つ最小単位」である。境界を引く基準は、レビュアーが隣のタスクを承認しながら、このタスクだけを差し戻せるかどうかにある。差し戻せないほど密結合な変更は 1 タスクにまとめ、逆に無関係な変更を無理に同居させない。

セットアップ・設定・雛形作成のような小さな下準備は、それを必要とする本体タスクに含めてよい。わざわざ単独タスクに切り出す必要はない。

## 依存関係の明示

すべてのタスクに `**Depends on:**` を書く。依存が 1 件でもリスト形式（`- Task N — 根拠: ...`）で書き、依存がないタスクも `**Depends on:** なし` と明示する。書き忘れと「依存なし」を区別できなければ、下流の並列判断が成立しないためである。

依存と認める根拠は次の 3 形式に限る。

1. このタスクの Consumes が、相手のタスクの Produces を参照している
2. Files に列挙した書き込み対象（Create / Modify / Test）が、相手のタスクと重なっている
3. 本節の plan 側ルールの名指し（例:「migration 直列ルール」）

「順番が自然」「先にやったほうが安心」は依存ではない。根拠のない依存（偽依存）は並列可能性を偽って狭めるプランの欠陥であり、根拠のない独立（依存の見落とし）はマージコンフリクトを生む欠陥である。どちらも同格に扱う。

タスク数が少なく直列で構わないプランは、プランヘッダの直下に「全タスクは直前タスクに依存する（チェーン）」と 1 行書けば、各タスクの記載を省略できる。記載のオーバーヘッドで小規模プランを重くしない。チェーン宣言と各タスクの個別記載は排他であり、混在させない。例外が必要になったら全タスクを個別記載に切り替える。プラン全体で記載が 0 件のものは旧形式として直列に扱われ、1 件でも記載があるのに欠けたタスクがあればプランの欠陥である。

並列化は目標ではなく副産物である。直列プランは欠陥ではない。無理な分割の兆候——雛形の重複・密結合の分割・インターフェースの過剰な事前確定——が出たら直列に倒す。

plan 側ルール:

- **ハブファイル集約ルール** — barrel・ルート登録・DI 登録のような「多くのタスクが 1 行ずつ足したくなるファイル」への追記は、1 つのタスクに集約して他タスクとの書き込み重なりを消す。形は 2 つあり、プロジェクトの流儀で選ぶ。(A) 先行の雛形タスクが、自ら Create するコンパイル可能なスタブを登録する（存在しないものは登録しない。コードのスタブは TDD の最小実装として正当であり、プラン文書のプレースホルダ禁止とは別物である）。(B) 後続の統合タスクが、完成したコンポーネントをまとめて登録する。集約しない場合は、ファイル重なり = 依存として直列に繋がることを受け入れる
- **migration 直列ルール** — migration 作成タスク同士は直列に繋ぐ。ファイルは重ならなくても、スキーマという共有リソースの適用順が意味を持つためである
- **生成物も書き込み対象** — タスクの作業がコミットされる生成物（lockfile・スナップショット・migration メタデータなど）を変更する場合、それも Files に列挙する。列挙漏れは依存の見落としと同じ欠陥として扱う

## 受け入れ基準

各タスクには、検証すべき挙動をテスト名レベルで列挙した「受け入れ基準」と、それを確認する検証コマンド（Run）を必ず与える。

- 1 行 1 挙動で、`- [ ]` のチェックボックスとして書く。この列挙が進捗追跡の単位である
- 境界値・異常系も欄を分けず、同じリストに挙動として書く
- 「何を検証するか」はここで確定する。「どう実装するか」「テストをどう書くか」は実装者の裁量であり、実行時に `kata:tdd` が列挙を先頭から 1 挙動ずつ RED-GREEN で消化する

## プランヘッダのテンプレート

プラン文書の冒頭は、必ず次の形式で始める。

```markdown
# <機能名> 実装計画

> **For agentic workers:** 本プランの実行には `kata:execute` が必須のサブスキルである。タスクは受け入れ基準の `- [ ]` チェックボックスで進捗を追跡する。

**Goal:** <この実装が何を達成するかを1文で>

**Architecture:** <採用するアプローチを2〜3文で>

**Tech Stack:** <使用する主要な技術・ライブラリ>

## Global Constraints

<spec が定めるプロジェクト全体の約束事をここへ書き写す。持ち込み禁止のライブラリ、
公開 API の互換性維持、ユーザー向け文言の表記規則、動作保証するランタイムの版、
といった類のもの。数値や名前は要約せず spec の記載どおりに写す。
各タスクの要件には、明記がなくてもこの節の内容が常に含まれる。>

---
```

## タスク構造のテンプレート

各タスクは次の形式で書く。

````markdown
### Task N: 送料計算

**背景:** 注文確定時にカート合計へ加算する送料を算出する。料金表は将来変更されるためハードコードではなく設定で持つ。

**Files:**
- Create: `src/shipping/fee.ts`
- Modify: `src/shipping/index.ts:12-18`
- Test: `tests/shipping/fee.test.ts`

**Interfaces:**
- Consumes: `loadConfig(): ShippingConfig`（Task 2 の Produces）
- Produces:
  ```typescript
  calculateShippingFee(weightKg: number): ShippingResult
  ```

**Depends on:**
- Task 2 — 根拠: `loadConfig`（Task 2 の Produces）を Consumes

**受け入れ基準:**
- [ ] 重量 5kg 以下なら送料 500 円を返す
- [ ] 重量 5kg 超なら 1kg ごとに 100 円を加算する
- [ ] 重量が負なら InvalidWeightError を投げる
- [ ] 重量 0 は正常系として送料 0 円を返す

Run: `npx vitest run tests/shipping/fee.test.ts`
````

- **背景** — このタスクが何のためにあるか、実装判断を左右する目的・制約を 1〜3 行で書く。タスク本文の自己完結を保つため、spec への参照では代替しない。3 行で足りないなら契約（Interfaces・受け入れ基準）の書き方を疑う
- **Interfaces** — Consumes は前段のタスクが Produces で公開した関数・型を、名前と型を変えずにそのまま使う。Produces は後続タスクが依存する公開関数・型であり、他のタスクの担当者はこのタスク本文まで読まないことが多く、ここに書いた名前と型がそのまま外部仕様になる
- **Depends on** — 依存が 1 件でもリスト形式で書く。依存がないタスクは `**Depends on:** なし` と 1 行で書く。認められる根拠は「依存関係の明示」節を参照
- **受け入れ基準** — 「受け入れ基準」節の規定に従う

## プレースホルダ禁止

次のような表現がプランに残っていたら、それはプランの失敗である。見つけた時点で書き直す。

- 「Task N と同様」という省略。実装者はタスクを番号順に読むとは限らないので、必要な契約は各タスクに完全な形で書く
- 読み手に「何を満たすべきか」を決めさせる指示。「適切にエラー処理」が欠陥なのは、どの入力に何を返すべきかという要求が読めないからである。「重量が負なら InvalidWeightError を投げる」のように要求が明確なら、実装方法が自由でも欠陥ではない
- 「TBD」「後で実装」をはじめとする先送り表現。プランは保存した時点で完結していなければならない
- 実装本体・テスト本体のコードブロック。プランに書いてよいコードブロックは、型定義・関数シグネチャ・spec からの転記断片・設定ファイル断片のみである。実装とテストの中身は実装者が書く
- Interfaces や受け入れ基準が、プランのどこにも宣言が現れない型・関数を参照している状態

## セルフレビュー

プランを書き終えたら、spec を読み直してプランと突き合わせる。サブエージェントに投げる作業ではなく、自分の目で行う。

1. **spec カバレッジ** — spec の各要件を、対応するタスクの受け入れ基準の行で 1 つずつ指させるか確認する。抜けがあれば受け入れ基準（必要ならタスク）を追加する
2. **プレースホルダ走査** — 「プレースホルダ禁止」節に挙げたパターンが残っていないか検索する。実装本体・テスト本体のコードブロックの混入もここで検出する
3. **Interfaces 突合** — 後続タスクの Consumes に書かれた名前・型が、依存先タスクの Produces の宣言と一致しているか確認する。あるタスクでは戻り値を `ShippingResult` 型で宣言したのに、別のタスクの Consumes では `{ fee: number }` という素のオブジェクトとして扱っている、といった食い違いは見落としやすい不整合の典型である
4. **依存の完全性** — 各タスクの Consumes に現れる名前が、その Depends on（推移閉包を含む）のタスクの Produces で定義されているか確認する。閉包の外の Produces を使っていたら依存の書き漏らしである
5. **ファイル重なり × 依存** — 書き込み対象（Create / Modify / Test・列挙された生成物）が重なるタスクの全ペアに、どちらか向きの依存（推移的でよい）があるか確認する。なければハブ集約・直列化・タスク統合のいずれかで解消する

見つけた問題はその場で inline 修正する。往復レビューにはしない。

## 実行への引き継ぎ

プランを保存したら、次の形で `kata:execute` の起動を提案する。

> 「plan を `docs/kata/plans/<filename>.md` に保存しました。`kata:execute` を起動して実装を進めますか？実行モード（自走 / サブエージェント駆動）と wave 並列の可否は execute が判断して宣言しますが、希望があればここで指定してください。」

実行方式をここで質問はしない。判断は `kata:execute` 側の役割である。ただしユーザーがこの時点で方式を指定した場合は、その指定が execute での判断に優先する。

## 関連スキル

- 承認済み spec の作成は `kata:design` が担当する。plan はその成果物を入力として受け取る
- 保存したプランの実行は `kata:execute` に引き継ぐ
